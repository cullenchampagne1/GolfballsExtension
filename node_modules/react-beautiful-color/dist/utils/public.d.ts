import type { ColorFormat, ColorInput, HexColor, HslaColor, HslColor, HsvaColor, HsvColor, RgbaColor, RgbColor } from '../types';
import { Color } from '../types';
/**
 * Convert hex color to RGB values
 * @param hex - Hex color string (e.g., "#ff6b9d")
 * @returns RGB color object with r, g, b values (0-255)
 */
export declare const hexToRgb: (hex: string) => RgbColor;
/**
 * Convert RGB values to hex color
 * @param rgb - RGB color object with r, g, b values (0-255)
 * @returns Hex color string (e.g., "#ff6b9d")
 */
export declare const rgbToHex: ({ r, g, b }: RgbColor) => string;
/**
 * Convert HSV color to RGB values
 * @param hsv - HSV color object with h (0-360), s (0-100), v (0-100)
 * @returns RGB color object with r, g, b values (0-255)
 */
export declare const hsvToRgb: ({ h, s, v }: HsvColor) => RgbColor;
/**
 * Convert RGB values to HSV color
 * @param rgb - RGB color object with r, g, b values (0-255)
 * @returns HSV color object with h (0-360), s (0-100), v (0-100)
 */
export declare const rgbToHsv: ({ r, g, b }: RgbColor) => HsvColor;
/**
 * Convert HSV color to hex string
 * @param hsv - HSV color object with h (0-360), s (0-100), v (0-100)
 * @returns Hex color string (e.g., "#ff6b9d")
 */
export declare const hsvToHex: (hsv: HsvColor) => string;
/**
 * Convert hex color to HSV values
 * @param hex - Hex color string (e.g., "#ff6b9d")
 * @returns HSV color object with h (0-360), s (0-100), v (0-100)
 */
export declare const hexToHsv: (hex: string) => HsvColor;
/**
 * Convert RGB values to HSL color
 * @param rgb - RGB color object with r, g, b values (0-255)
 * @returns HSL color object with h (0-360), s (0-100), l (0-100)
 */
export declare const rgbToHsl: ({ r, g, b }: RgbColor) => HslColor;
/**
 * Convert HSL color to RGB values
 * @param hsl - HSL color object with h (0-360), s (0-100), l (0-100)
 * @returns RGB color object with r, g, b values (0-255)
 */
export declare const hslToRgb: ({ h, s, l }: HslColor) => RgbColor;
/**
 * Convert HSL color to hex string
 * @param hsl - HSL color object with h (0-360), s (0-100), l (0-100)
 * @returns Hex color string (e.g., "#ff6b9d")
 */
export declare const hslToHex: (hsl: HslColor) => string;
/**
 * Convert hex color to HSL values
 * @param hex - Hex color string (e.g., "#ff6b9d")
 * @returns HSL color object with h (0-360), s (0-100), l (0-100)
 */
export declare const hexToHsl: (hex: string) => HslColor;
/**
 * Convert HSV color to HSL color
 * @param hsv - HSV color object with h (0-360), s (0-100), v (0-100)
 * @returns HSL color object with h (0-360), s (0-100), l (0-100)
 */
export declare const hsvToHsl: ({ h, s, v }: HsvColor) => HslColor;
/**
 * Convert HSL color to HSV color
 * @param hsl - HSL color object with h (0-360), s (0-100), l (0-100)
 * @returns HSV color object with h (0-360), s (0-100), v (0-100)
 */
export declare const hslToHsv: ({ h, s, l }: HslColor) => HsvColor;
/**
 * Get the contrast color (black or white) for a given hex color
 * @param hex - Hex color string (e.g., "#ff6b9d")
 * @returns "#000000" for light colors, "#ffffff" for dark colors
 */
export declare const getContrastColor: (hex: string) => string;
/**
 * Generate a random hex color
 * @returns Random hex color string (e.g., "#a1b2c3")
 */
export declare const randomHex: () => string;
/**
 * Format a ColorState object into a CSS color string
 * @param color - ColorState object
 * @param format - Output format: 'hex', 'rgb', 'rgba', 'hsl', 'hsla'
 * @returns Formatted color string
 */
export declare const formatColorString: (color: Color, format?: Exclude<ColorFormat, "hsv" | "hsva">) => string;
export declare function convertColor(color: ColorInput, format: 'hex'): HexColor;
export declare function convertColor(color: ColorInput, format: 'rgb'): RgbColor;
export declare function convertColor(color: ColorInput, format: 'rgba'): RgbaColor;
export declare function convertColor(color: ColorInput, format: 'hsl'): HslColor;
export declare function convertColor(color: ColorInput, format: 'hsla'): HslaColor;
export declare function convertColor(color: ColorInput, format: 'hsv'): HsvColor;
export declare function convertColor(color: ColorInput, format: 'hsva'): HsvaColor;
