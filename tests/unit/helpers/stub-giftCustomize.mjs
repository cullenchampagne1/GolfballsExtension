/**
 * Stub for src/modals/giftCustomize.jsx (JSX — node cannot import it).
 * proposalEmailSource.js only uses colorNameOf(hex) → display name. The
 * stub returns '' (the real function's "unknown hex" result), so tests
 * assert on colorHex passthrough, never on a stubbed color name.
 */
export function colorNameOf() { return ''; }
